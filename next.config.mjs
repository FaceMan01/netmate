/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "sun9-50.userapi.com",
            "i.ytimg.com"
        ]
    }
};

export default nextConfig;
